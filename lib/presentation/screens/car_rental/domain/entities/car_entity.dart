class CarEntity {
  int? statusCode;
  String? message;
  String? token;
  num? roleType;
  num? userId;
  CarEntity(
      {this.statusCode, this.message, this.token, this.roleType, this.userId});
}
